{
  "name": "xs40-keymap-tauri",
  "private": true,
  "version": "1.0.0",
  "description": "XS40_CH552T 键位映射编辑器 (Tauri 桌面版)",
  "scripts": {
    "build": "npx esbuild@0.21.5 src/app.js --bundle --format=iife --outfile=dist/bundle.js && cp src/index.html src/styles.css dist/",
    "dev": "npx esbuild@0.21.5 src/app.js --bundle --format=iife --outfile=dist/bundle.js --watch",
    "tauri": "cargo tauri",
    "tauri-dev": "npm run build && cargo tauri dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}